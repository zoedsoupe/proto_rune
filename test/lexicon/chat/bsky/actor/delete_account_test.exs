defmodule Lexicon.Chat.Bsky.Actor.DeleteAccountTest do
  use ExUnit.Case, async: true

  # Simple test to ensure the module exists
  test "module exists" do
    assert Code.ensure_loaded?(Lexicon.Chat.Bsky.Actor.DeleteAccount)
  end
end
